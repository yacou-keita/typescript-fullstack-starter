mutation Login($input: UserInput!){
  login(input: $input) {
    errors {
      field
      message
    }
    user {
      id,
      firstname,
      lastname
      username
      createdAt
      updatedAt
    }
  }
}